enum ChatRole {
  USER = "user",
  AI = "ai",
}
enum ChatType {
  FORM = "form",
  TEXT = "text",
}
enum ChatFormType {
  WRITING = "writing",
  MATH = "math",
}

export { ChatRole, ChatType, ChatFormType };
